# 日课 · 云端接入说明

页面本身默认是**完全离线**的：数据只在你这台设备的浏览器里，一个字节都不外发。下面三档是可选的，用哪档随你，随时可以退回来。

| | 要做什么 | 好处 | 代价 |
|---|---|---|---|
| **不接云**（默认） | 什么都不用做 | 数据绝不出设备 | 理解靠规则引擎，看不懂复杂表达 |
| **直连官方 API** | 选服务商 → 填 Key | 5 分钟搞定，理解力提升明显 | Key 存在这台设备上；换设备要重填 |
| **自建代理**（推荐） | 部署一个 Cloudflare Worker | Key 留在服务端；手机电脑可同步 | 要注册 CF 账号，约 20 分钟 |

---

## 一、直连官方 API

日课 → 我的 → AI 增强 → 打开开关 → 服务商下拉选一家 → **只填 Key** → 点「测试连接」。

内置的地址与模型（各家会调整，连不通时以服务商官网文档为准）：

| 服务商 | 接口地址 | 模型 |
|---|---|---|
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash`（有免费额度） |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| 硅基流动 | `https://api.siliconflow.cn/v1` | `Qwen/Qwen2.5-7B-Instruct` |
| Groq | `https://api.groq.com/openai/v1` | `llama-3.1-8b-instant`（境外） |
| 本机 Ollama | `http://localhost:11434/v1` | `qwen2.5:7b` |

**注意**：线上页面是 https 的，浏览器会拦截 http 的本机 Ollama。要用本机模型请改用离线版（`日课-离线版.html`）。

### 两个开关的区别

- **启用大模型分析**：总开关。关掉就完全回到规则引擎。
- **让模型理解我的话**：开着时模型负责**读懂你的话并填字段**（情绪分值、睡了几小时、运动多久、压力源）；关掉则模型只负责**写回应**，字段仍由本地规则抽。前者准得多，后者快且省 token。

任一步失败都会**静默回退**到本地规则，不会报错、不会丢记录。回应卡片下方会标出这次是「模型理解」还是「规则抽取」，一眼能看出来。

---

## 二、自建代理（推荐）

一个 Cloudflare Worker 同时解决两件事：**Key 不落在设备上**，**手机和电脑共用一份记录**。

### 部署

1. 注册 [cloudflare.com](https://cloudflare.com)，进 **Workers & Pages → Create → Worker**，随便起个名先部署一次。
2. 建 KV：**Workers & Pages → KV → Create namespace**，名字随意。
3. 进 Worker → **Settings → Bindings → Add → KV namespace**：变量名必须填 `RIKKE`，选刚建的 namespace。
4. 进 Worker → **Settings → Variables → Add**，加三个（都建议点 Encrypt）：

   | 变量名 | 填什么 |
   |---|---|
   | `APP_TOKEN` | 你自己想一个口令，比如一串随机字符。前端的 Key 就填它 |
   | `UPSTREAM` | 上游接口基址，如 `https://api.deepseek.com` |
   | `UPSTREAM_KEY` | 上游给你的真实 API Key |

5. Worker → **Edit Code**，把本仓库 `worker.js` 的内容整段粘进去，点 Deploy。

### 接回日课

我的 → AI 增强 → 服务商选 **自定义**：

- 接口地址：`https://你的worker名字.你的子域.workers.dev/v1`
- 模型名：填上游要的那个，比如 `deepseek-chat`
- API Key：填你的 `APP_TOKEN`（不是上游的真 Key）

点「测试连接」，看到「理解 + 回应都正常」就成了。

### 多设备同步

填好地址后，「数据与同步」卡片里有两个按钮：

- **同步（双向）**：先把云端拉下来跟本地合并，再把合并结果推回去。**平时就点这个。**
- **只从云端拉取**：只下不上，适合"看看云端有什么、但先不动云端"的场景。

合并规则：**按条目 id 对齐、同一条按时间戳取新**。所以：

- 手机记 10 条、电脑记 5 条，无论谁先谁后，同步完两边都是 15 条，**不会互相冲掉**；
- 重复点同步不会让记录翻倍；
- **删除也会同步**——A 设备删掉一条，B 设备同步后也会删掉，不会"删了又活过来"。

> 早期版本的「上传」是拿本地整包覆盖云端，如果另一台设备记了东西没同步，会被冲掉。这个行为已经修掉了，现在上传前一定先合并。

### 电脑关机会影响同步吗

**不会。** 代理跑在 Cloudflare 的边缘节点上，7×24 在线，跟你电脑开不开机没关系。你的电脑和手机都只是它的客户端。

但有一个例外要注意：如果模型那一项你选的是**本机 Ollama**（`http://localhost:11434`），那 AI 理解和回应走的是你电脑上的模型——**电脑关机时手机上的 AI 功能会失败**，会自动回退到本地规则引擎，记录和同步本身不受影响。想让手机也用得上 AI，就把 `UPSTREAM` 配成云端模型服务商。

---

## 三、关于隐私，几句实话

- 记录里是你的情绪、睡眠、家庭琐事，按《个人信息保护法》属于**敏感个人信息**。
- 不接云时它一个字节都不外发；接了云，你的原文会经过模型服务商。这笔账要自己认。
- 走自建代理时，日记存在 Cloudflare KV 里是**明文**的。介意的话就只用代理功能、不开同步，多设备迁移继续用「备份 JSON」。
- 无论哪种方式，导出的 Markdown / JSON 里都不会包含你的 API Key。

---

## 接口一览（自建代理时）

| 路径 | 方法 | 作用 |
|---|---|---|
| `/v1/chat/completions` | POST | 转发给上游模型，需要 `X-App-Token` 头 |
| `/api/sync` | GET | 拉取云端记录，返回 `{entries, tombs, ts}` |
| `/api/sync` | PUT | 上传记录，body 为 `{entries:[...], tombs:[...]}`，上限 5000 条 |

`tombs` 是"已删除条目的 id 列表"，用于防止删除被别的设备推回来。服务端对 tombs 取**并集**（只增不减），上限 2000 条。旧版本存的数据没有这个字段，服务端会自动补空数组，不用做迁移。
