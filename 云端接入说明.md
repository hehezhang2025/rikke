# 日课 · 云端接入说明

页面默认是**完全离线**的：数据只在这台设备的浏览器里，一个字节都不外发。下面这些全是可选的，随时能退回来。

## 先分清两件事

很多人把这两件事混在一起，其实它们可以完全独立地选：

| 你想解决什么 | 用什么 | 要注册新账号吗 |
|---|---|---|
| **手机和电脑共用一份记录**（同步） | GitHub 私有仓库 | 不用，你已经有 GitHub 账号了 |
| **让 AI 读懂你的话**（理解力） | 直连模型服务商，或自建代理 | 直连要注册服务商；代理要注册 Cloudflare |

只用同步、不接 AI —— 完全可以。只用 AI、不同步 —— 也完全可以。

---

# 一、同步：GitHub 私有仓库（免费，零部署）

## 它是什么

日课把你的记录打包成一个 JSON 文件，存在**你自己 GitHub 账号下的一个私有仓库**里。私有仓库只有你看得见，GitHub 的员工在未经授权的情况下也不能访问内容。

没有服务器、没有部署、没有月费，也不用注册任何新服务——**你部署日课用的就是 GitHub，账号现成的。**

## 三步配好

1. **建一个私有仓库**
   去 [github.com/new](https://github.com/new) 建，名字随意（比如 `rikke-data`），**一定要选 Private**。
   懒得去网页点也行：在日课里填好令牌后按「帮我建私有仓库」，它自己建。

2. **生成一个令牌**
   去 [github.com/settings/tokens](https://github.com/settings/tokens) → **Generate new token → Fine-grained token**，然后：
   - Repository access 选 **Only select repositories**，勾上刚建的那个仓库
   - Permissions 里只开一项：**Contents → Read and write**
   - 到期日建议选 1 年（Fine-grained 最长就是 1 年）

   为什么强调"只开一项"：这个令牌会存在你手机和电脑的浏览器里。万一泄露，只有这一个仓库的权限，够不到你别的代码。

3. **填进日课**
   我的 → 数据与同步 → 云端放哪儿选 **GitHub 私有仓库** → 填仓库名（`用户名/仓库名`）和令牌 → 点「测试并初始化」。

手机和电脑**填同一个仓库、同一个令牌**，就成了。

## 自动同步

默认开着：

- 每记完一条、每删完一条 —— 后台静默同步一次，不弹窗、不打断你
- 打开页面时 —— 先静默拉一次云端
- 从别的 App 切回日课时 —— 补一次
- 断网了 —— 不打扰你，本地照记，联网后自动补上

状态灯就在卡片上：**已同步 / 同步中 / 待同步 / 未配置**，一眼能看清。想改成手动，关掉「记完自动同步」开关即可，关了之后点按钮才同步。

连续记几条不会发好几个请求——1.2 秒内的操作会合并成一次。

## 同步规则

- **按条目 id 对齐、同一条按时间戳取新**。手机记 10 条、电脑记 5 条，同步完两边都是 15 条，不会互相冲掉。
- **删除也会同步**。A 设备删掉的，B 设备同步后也删掉，不会"删了又活过来"。
- **两台设备同时记不会打架**。撞车时自动重试最多 3 次，还不行才报"待同步"，绝不静默吞数据。
- **云端数据会被清洗**。如果文件被手工改坏、或者混进了旧版本数据，缺字段的自动补齐、完全坏的丢弃，不会让页面白屏。

> 早期版本的「上传」是拿本地整包覆盖云端，另一台设备记的东西会被冲掉。这个行为已经修掉了。

## 容量与费用

- 单个文件建议 1 MB 以内。一条记录大约 300–600 字节，**够记好几千条**，正常人用好几年。
- GitHub 免费账号私有仓库数量不限，API 限速 5000 次/小时。自动同步一天最多几十次，离上限很远。
- 每次同步会产生一个 commit，所以仓库自带完整的修改历史——相当于白送一个版本备份。

---

# 二、AI 增强：让模型读懂你的话

不配也能用，就是理解力停在规则引擎那一档（看不懂"五个半小时"这类表达，也分不清"被批了但还行"）。

## 2.1 直连官方 API（5 分钟）

日课 → 我的 → AI 增强 → 打开开关 → 服务商下拉选一家 → **只填 Key** → 测试连接。

| 服务商 | 接口地址 | 模型 |
|---|---|---|
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash`（有免费额度） |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| 硅基流动 | `https://api.siliconflow.cn/v1` | `Qwen/Qwen2.5-7B-Instruct` |
| Groq | `https://api.groq.com/openai/v1` | `llama-3.1-8b-instant`（境外） |
| 本机 Ollama | `http://localhost:11434/v1` | `qwen2.5:7b` |

想零成本试，选**智谱 GLM**，去 open.bigmodel.cn 拿个 Key 就行。

**注意**：线上页面是 https 的，浏览器会拦截 http 的本机 Ollama。要用本机模型请改用离线版 `日课-离线版.html`。

### 两个开关

- **启用大模型分析**：总开关。关掉就完全回到规则引擎。
- **让模型理解我的话**：开着时模型负责**读懂并填字段**（情绪分值、睡了几小时、运动多久、压力源）；关掉则模型只**写回应**，字段仍由本地规则抽。前者准得多，后者快且省 token。

任一步失败都**静默回退**到本地规则，不报错、不丢记录。回应卡片下方会标出这次是「模型理解」还是「规则抽取」。

## 2.2 自建代理（Key 不落设备）

嫌 Key 存在浏览器里不放心，就套一层 Cloudflare Worker：Key 留在服务端，前端拿不到。

**Cloudflare 是什么**：一家做 CDN 和边缘计算的美国公司，全球有 300 多个机房。它的 **Workers** 服务可以理解为"把你的一段代码复制到全世界所有机房同时运行"——用户访问时，由离他最近的机房响应，所以又快又不会因为某台机器宕机而挂掉。免费额度是每天 10 万次请求，个人用根本用不完。

### 部署

1. 注册 [cloudflare.com](https://cloudflare.com)，进 **Workers & Pages → Create → Worker**，随便起个名先部署一次。
2. 进 Worker → **Settings → Variables → Add**，加三个（都建议点 Encrypt）：

   | 变量名 | 填什么 |
   |---|---|
   | `APP_TOKEN` | 你自己想一个口令。前端的 Key 就填它 |
   | `UPSTREAM` | 上游接口基址，如 `https://api.deepseek.com` |
   | `UPSTREAM_KEY` | 上游给你的真实 API Key |

3. Worker → **Edit Code**，把 `worker.js` 整段粘进去，Deploy。

### 接回日课

我的 → AI 增强 → 服务商选**自定义**：

- 接口地址：`https://你的worker名字.你的子域.workers.dev/v1`
- 模型名：填上游要的那个，比如 `deepseek-chat`
- API Key：填 `APP_TOKEN`（不是上游真 Key）

如果还想让它兼管同步，就再建个 KV namespace、变量名填 `RIKKE`，然后把「云端放哪儿」选成**自建代理**、接口地址填 `https://你的worker名字.你的子域.workers.dev`。不过既然有 GitHub 那条免费的路，一般没必要。

---

# 三、电脑关机影响同步吗

**不影响。** 无论用 GitHub 还是 Cloudflare，数据都存在云端服务器上，7×24 在线。你的电脑和手机都只是客户端，关机跟同步没关系。

唯一例外：模型那一项如果选的是**本机 Ollama**（`localhost:11434`），那 AI 走的是你电脑上的模型——**电脑关机时手机上的 AI 会失败**，自动回退规则引擎，记录和同步照常。想让手机也用上 AI，就改用云端模型服务商。

---

# 四、关于隐私，几句实话

- 记录里有情绪、睡眠、家庭琐事，按《个人信息保护法》属于**敏感个人信息**。
- 不接云时它一个字节都不外发。接了 AI，你的原文会经过模型服务商——这笔账要自己认。
- GitHub 私有仓库有访问控制，但内容是**明文**的。Cloudflare KV 同理。
- 两个都介意的话：只用 GitHub 做同步（原文不出你的账号），AI 那栏留空。
- 无论哪种方式，导出的 Markdown / JSON / CSV 里都不含令牌。

---

# 五、接口一览（自建代理时）

| 路径 | 方法 | 作用 |
|---|---|---|
| `/v1/chat/completions` | POST | 转发给上游模型，需要 `X-App-Token` 头 |
| `/api/sync` | GET | 拉取云端记录，返回 `{entries, tombs, ts}` |
| `/api/sync` | PUT | 上传记录，body 为 `{entries:[...], tombs:[...]}`，上限 5000 条 |

`tombs` 是已删除条目的 id 列表，防止删除被别的设备推回来。服务端对 tombs 取**并集**（只增不减），上限 2000 条。旧版本存的数据没有这个字段，服务端自动补空数组，不用迁移。
